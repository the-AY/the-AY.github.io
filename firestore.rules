rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow user to read/write their own logs
      // This is a basic rule set; refine it based on actual auth needs
      allow read, write: if request.auth != null;
    }
  }
}
